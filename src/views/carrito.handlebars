<h1>Carrito</h1>
<table style="width: 100%;">
    <thead>
        <tr>
            <th>#</th>
            <th>Titulo</th>
            <th>Code</th>
            <th>Category</th>
            <th>Cantidad</th>
            <th>Price</th>
        </tr>
    </thead>
    <tbody>
        {{#each productos}}
        <tr>
            <td><img src="{{this.thumbnail}}" alt="{{this.title}}"  class="product-image"></td>
            <td>{{this.title}}</td>
            <td>{{this.code}}</td>
            <td>{{this.category}}</td>
            <td>{{this.quantity}}</td>
            <td>${{this.price}}</td>
            <td><button class="btn btn-danger" id="{{this._id}}">Eliminar</button></td>
        </tr>
        {{/each}}
    </tbody>
</table>
<button class="btn btn-warning" id="vaciar">Vaciar Carrito</button>